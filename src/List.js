// import React, {useState, useEffect} from 'react';
// import Button from './Button';
// import TodoItem from './TodoItem'

// function List(props) {
//     return (
//         <>
//           {props.list && props.list.map((item, idx) => {  
//                                 return <TodoItem key={props.idx} item={props.item} completedClassName={props.completedClassName} onCompletedClick={props.onCompletedClick}
//                                     handleDeleteButton={props.handleDeleteButton}/>
//                             })}  
//      </>
//     );
// }

// export default List;
// // someFunc={props.getStartedButton}